<Window x:Class="MusicBrowser.SearchResult"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MusicBrowser"
        mc:Ignorable="d"
        Title="Search Results" Height="300" Width="594" WindowStartupLocation="CenterScreen">
    <Grid>
        <DataGrid x:Name="SearchData" Margin="10" Width="566" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name"  Width="200" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="Extension" Width="100" Binding="{Binding Extension}"/>
                <DataGridTextColumn Header="Path" Binding="{Binding Filepath}"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Window>
